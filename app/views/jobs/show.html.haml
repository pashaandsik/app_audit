-a= Service.where(car_id: @job.car_id)
-if(@job.repair_job)
  =link_to 'В ремонт', new_repair_url(car_id: @job.car_id, job_id: @job.id ), :class => 'a_baton'
-elsif(@job.to)
  - a.each do |er|
    =link_to 'В техническое обслуживание', edit_service_path(er.id), :class => 'a_baton'
-else
  .text
    Километров пройдено
    %p=@job.km_n
    Мото часов
    %p=@job.m_ch

= link_to 'Редактировать', edit_job_path(@job) , :class => "btn btn-info" unless @job.to or @job.repair
= link_to 'Назад', jobs_path, :class => "btn" unless @job.to or @job.repair
