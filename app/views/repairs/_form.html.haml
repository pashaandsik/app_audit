
= simple_form_for(@repair) do |f|
  = f.error_notification
  = f.hidden_field :car_id
  = f.hidden_field :job_id

  .form-inputs
    = f.input :replacement, label: 'Заменить?'
    = f.collection_select(:detail_id, Detail.where("car_id = ? ", params[:car_id]), :id, :name_det, prompt: true)
    = f.input :cause, label: 'Причина'

  .form-actions
    = f.submit class: ["btn btn-primary"]
