
= simple_form_for(@repair) do |f|
  = f.error_notification
  = f.hidden_field :car_id
  = f.hidden_field :detail_id

  .form-inputs
    = f.input :replacement, label: 'Замернить?'
    = f.input :cause, label: 'Причина'





  .form-actions
    = f.submit class: ["btn btn-primary"]
